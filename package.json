{
  "name": "graphql-course-starter",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "dev": "turbo run start",
    "build": "turbo run build",
    "test": "turbo run test:fe test:be",
    "codegen": "turbo run codegen"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
